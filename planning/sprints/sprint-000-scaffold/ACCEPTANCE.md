# Sprint 000 Acceptance Criteria

Sprint 000 is complete when:

- app runs locally;
- NSML WorkDesk shell renders;
- dashboard visible at / or /dashboard;
- sidebar/navigation visible;
- all five workspaces visible;
- three vessel pages render separately;
- Projects page renders;
- Other page renders;
- Import staging area renders;
- Cases placeholder renders;
- Drafts placeholder renders;
- Writing Style placeholder renders;
- CoS Assistant placeholder renders;
- mock Imported Email Workbench renders;
- workspace-level vessel/project/other correspondence surfaces render;
- linked correspondence placeholder in Cases renders;
- `/import` is staging/unclassified only, not permanent email storage;
- mock data only is used for Sprint 000 correspondence views;
- no Outlook integration exists;
- no email sending exists;
- no AI workflow exists;
- no database runtime dependency is required;
- no EML parsing exists yet;
- no persistence exists yet.
